export async function runEmployeeSyncWorkflow({ recordId, employeeType, context }) {
  void recordId;
  void employeeType;
  void context;
  throw new Error("Employee sync workflow not implemented yet");
}
